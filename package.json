{
  "name": "axonswap",
  "version": "1.0.0",
  "private": true,
  "description": "AxonSwap — The native DEX on Axonchain",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "lint": "cd frontend && npm run lint",
    "contracts:compile": "cd contracts && npx hardhat compile",
    "contracts:test": "cd contracts && npx hardhat test",
    "contracts:deploy": "cd contracts && npx hardhat run scripts/deploy.js --network axon",
    "contracts:verify": "cd contracts && node scripts/verify.js",
    "contracts:seed": "cd contracts && npx hardhat run scripts/seed-pools.js --network axon"
  }
}
